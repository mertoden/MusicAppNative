import { StyleSheet } from "react-native"

export default StyleSheet.create({
    container: {
        backgroundColor: '#eceff1',
        padding: 9,
        marign: 5,
        borderRadius: 5,
    },
});